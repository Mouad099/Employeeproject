server:
  port: ${PORT:8080}

spring:
  profiles:
    active: ${APP_ENV:development}
  data:
    rest:
      base-path: /api
  mvc:
    log-resolved-exception: false
  h2:
    console:
      enabled: false
  
  datasource:
    url: jdbc:postgresql://localhost:5432/NewDB
    username: postgres
    password: mouadit2023
  driver-class-name: org.postgresql.Driver








